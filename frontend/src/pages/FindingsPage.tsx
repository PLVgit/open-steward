import { PlaceholderPage } from "@/components/PlaceholderPage";

export function FindingsPage() {
  return (
    <PlaceholderPage
      title="Findings"
      description="Structural, SQL and reconciliation findings for the pipeline."
      ticket="Ticket 16"
    />
  );
}
