import { PlaceholderPage } from "@/components/PlaceholderPage";

export function StatisticsPage() {
  return (
    <PlaceholderPage
      title="ETL Statistics"
      description="Per-job row counts, loss and primary-key metrics."
      ticket="Ticket 17"
    />
  );
}
